# Base URL of the LR Time backend (Django).
# In development, use the LAN IP of the machine running the dev backend
# (your phone with Expo Go must reach it on the same Wi-Fi network).
# Example: EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:8000
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:8000
